"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkUsedClasses = (styleSources, classNames) => {
    const styleImportsRegex = /(?:import|require).*['"](.*(?:styl|css|scss|less))['"]/g;
    let match = styleImportsRegex.exec(source);
    const importedStyleNames = [];
    while (match != null) {
        importedStyleNames.push(match[1]);
        match = styleImportsRegex.exec(source);
    }
    const importedStylePaths = importedStyleNames.map(name => resolve(this.context, name));
    const styleSourcePromises = importedStylePaths.map(path => readFile(path));
    const sources = yield Promise.all(styleSourcePromises);
    const parsedSources = sources.map(source => source.toString());
};
